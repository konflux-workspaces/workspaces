---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: internalworkspaces.workspaces.konflux.io
spec:
  group: workspaces.konflux.io
  names:
    kind: InternalWorkspace
    listKind: InternalWorkspaceList
    plural: internalworkspaces
    singular: internalworkspace
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.visibility
      name: Visibility
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: InternalWorkspace is the Schema for the workspaces API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InternalWorkspaceSpec defines the desired state of Workspace
            properties:
              owner:
                description: UserInfo collects information for a logged user
                properties:
                  jwtInfo:
                    description: JwtInfo collects information from the User JSON Web
                      Token
                    properties:
                      email:
                        type: string
                      sub:
                        type: string
                      username:
                        type: string
                    required:
                    - sub
                    - username
                    type: object
                required:
                - jwtInfo
                type: object
              visibility:
                type: string
            required:
            - owner
            - visibility
            type: object
          status:
            description: WorkspaceStatus defines the observed state of Workspace
            properties:
              space:
                type: string
            required:
            - space
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
