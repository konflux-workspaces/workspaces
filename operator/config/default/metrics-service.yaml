apiVersion: v1
kind: Service
metadata:
  name: controller-manager-metrics
  namespace: system
spec:
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  selector:
    control-plane: controller-manager
  type: ClusterIP
  sessionAffinity: None
