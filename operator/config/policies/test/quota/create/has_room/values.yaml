apiVersion: cli.kyverno.io/v1alpha1
kind: Value
metadata:
  name: values
globalValues:
  request.operation: CREATE
policies:
  - name: workspace-quota
    rules:
      - name: quota-same-user
        values:
          ownedWorkspaces: 0
