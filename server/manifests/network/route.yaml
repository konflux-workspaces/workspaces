apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    provider: workspaces
    run: rest-api-server
  name: rest-api-server
  namespace: workspaces-system
spec:
  host: ''
  port:
    targetPort: 8080
  to:
    kind: Service
    name: rest-api-server
    weight: 100
  tls:
    termination: edge
  wildcardPolicy: None
